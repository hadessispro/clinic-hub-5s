{
  "name": "clinic-hub-5s",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend:build": "pnpm --dir apps/backend build",
    "backend:dev": "pnpm --dir apps/backend start:dev",
    "backend:start": "pnpm --dir apps/backend start:prod",
    "location:update": "node scripts/update-clinic-location.mjs",
    "provision:pvc": "node scripts/provision-pvc-users.mjs",
    "deploy:vps": "bash scripts/deploy-vps-hostinger.sh"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
